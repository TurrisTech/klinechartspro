[serve.static]
# Not plain "bun-plugin-svelte": see scripts/svelte-plugin.ts for why the dev server needs
# Svelte's preserveWhitespace forced off (bits-ui Popover renders nothing without it).
plugins = ["./scripts/svelte-plugin.ts"]
